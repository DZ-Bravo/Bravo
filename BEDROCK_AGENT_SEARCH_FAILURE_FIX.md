# Bedrock Agent 검색 실패 무한 루프 해결 가이드

## 문제 상황
- Knowledge Base 검색 결과가 비어있을 때 (`retrievedReferences: []`)
- Agent가 동일한 검색을 반복하여 무한 루프 발생

## 해결 방법: Agent Instruction 수정

### 추가해야 할 내용

Agent Description 또는 Instruction 섹션에 다음 내용을 추가하세요:

```
[검색 실패 처리 규칙]

Knowledge Base 검색을 수행한 후:
1. 검색 결과 확인: retrievedReferences 배열이 비어있거나 길이가 0인 경우, 검색이 실패한 것으로 간주합니다.
2. 검색 실패 시 처리:
   - 추가 검색을 시도하지 마세요.
   - 즉시 최종 답변을 반환합니다.
   - 다음 문장만 출력: "요청하신 지역에 해당하는 등산로 정보를 찾지 못했습니다."
3. 검색 시도 제한:
   - 동일한 쿼리로 검색을 2회 이상 반복하지 마세요.
   - 검색 결과가 비어있으면 즉시 최종 답변으로 진행합니다.

[작업 순서 수정]

Knowledge Base 검색 후:
- 검색 결과가 비어있는 경우 → 즉시 최종 답변 반환 (추가 검색 금지)
- 검색 결과가 있는 경우 → 기존 필터링 및 정렬 프로세스 진행
```

### 구체적인 수정 예시

기존 "[작업 순서]" 섹션의 "Knowledge Base 검색" 부분을 다음과 같이 수정:

```
Knowledge Base 검색
1. 사용자 입력에서 위 규칙에 따라 주요 지역명 1개 이상을 추출합니다.
2. KB 검색 시에는 추출한 지역명을 사용해 "지역명 산" 형태 등으로 검색합니다.
3. 검색 결과 확인:
   - 검색 결과가 비어있거나 (retrievedReferences가 빈 배열) 검색이 실패한 경우:
     → 추가 검색을 시도하지 말고 즉시 최종 답변으로 진행
     → "요청하신 지역에 해당하는 등산로 정보를 찾지 못했습니다." 출력
   - 검색 결과가 있는 경우:
     → KB 검색 결과 상위 3개 chunk까지만 사용
     → 이후 필터링 및 정렬 프로세스 진행
```

### 추가 권장 사항

"[제한 사항]" 섹션에 다음 내용 추가:

```
- Knowledge Base 검색 결과가 비어있는 경우, 추가 검색을 시도하지 말고 즉시 "요청하신 지역에 해당하는 등산로 정보를 찾지 못했습니다."를 반환합니다.
- 동일한 검색 쿼리를 반복하지 마세요.
```

## AWS 콘솔에서 수정 방법

1. AWS Bedrock 콘솔 접속
2. Agents → "Hiker_course_recommendation" (EFHTPK8I6B) 선택
3. Agent builder → Instructions 또는 Advanced prompts 섹션
4. 위 내용을 Instruction에 추가
5. Save 후 테스트

## 테스트

수정 후 다음 쿼리로 테스트:
- "서울 근처 쉬운 등산 코스 추천해줘"

예상 결과:
- Knowledge Base 검색 결과가 비어있으면 → "요청하신 지역에 해당하는 등산로 정보를 찾지 못했습니다." 메시지 반환
- 무한 루프 없이 한 번의 검색 시도 후 최종 답변 반환
