# 1. 프로젝트 개요

## 1.1 개발 배경

### (1) 문제 정의

현대 사회에서 등산은 많은 사람들에게 건강한 여가 활동이자 자연과의 교감을 나누는 중요한 취미 활동 중 하나로 자리 잡고 있다. 특히 한국은 산악 지형이 발달하여 전국 각지에 다양한 등산로와 아름다운 자연 경관을 제공하는 산들이 산재해 있다. 그러나 등산을 처음 시작하는 사람이나 특정 지역의 등산 정보를 찾고자 하는 사람들은 종종 어디를 가야 할지, 어떤 코스가 자신의 체력과 경험 수준에 맞는지 고민하게 된다.

또한, 등산 코스의 난이도, 거리, 소요 시간, 현재 날씨 상황 등 여러 정보를 일일이 검색하고 비교하는 것은 매우 번거롭고 시간 소모적인 일이다. 많은 사람들은 이 과정을 단축시키고, 보다 효율적으로 등산 계획을 세우고자 하는 욕구를 가지고 있다. 특히, 주말이나 휴가를 효과적으로 활용하기 위해서는 개인 맞춤형 등산 코스와 실시간 정보가 제공되는 서비스가 절실히 필요하다.

현재 제공되고 있는 대부분의 등산 관련 서비스들은 정적인 정보 제공에 그치는 경우가 많다. 이는 사용자들이 실시간으로 변화하는 날씨 상황이나 자신의 체력 수준에 맞춰 유연하게 대응하기 어렵게 만든다. 예를 들어, 특정 등산로가 날씨로 인해 위험하거나, 초보자에게는 너무 어려운 코스일 경우, 이를 빠르게 파악하고 대안을 제시할 수 있는 서비스가 부족하다.

또한, 등산을 즐기는 사람들은 자신이 다녀온 산과 코스를 기록하고 관리하고 싶어 하며, 다른 등산객들과 정보를 공유하고 싶어 한다. 그러나 이러한 기능을 통합적으로 제공하는 서비스는 매우 제한적이다.

이러한 문제를 해결하기 위해서는 실시간 데이터와 개인화된 추천을 결합한 혁신적인 서비스가 필요하다. 우리의 프로젝트는 이러한 요구를 충족시키기 위해 공공 데이터를 이용하여 전국 등산로 정보를 제공하고, 실시간 날씨 정보를 반영하며, 인공지능(AI)이 사용자의 지역과 난이도 선호에 따라 최적의 등산 코스를 생성해주는 서비스를 개발하고자 한다.

### (2) 요구사항 정의

앞서 언급한 문제를 해결하기 위해 다음 항목을 만족하는 서비스를 개발하고자 했다.

첫째, 사용자가 원하는 지역(도, 시, 군, 구, 읍, 면, 동, 리 등 모든 행정구역)과 난이도(쉬움, 보통, 어려움)를 입력하면 해당 지역의 등산 코스를 추천한다.

둘째, 실시간 날씨 정보를 조회하여 위험한 날씨 조건(천둥번개, 강풍 등)을 필터링하고, 날씨가 좋은 코스를 우선적으로 추천한다.

셋째, Knowledge Base에 저장된 전국 등산로 정보를 벡터 검색을 통해 효율적으로 검색하고, 지역 필터링을 통해 정확한 지역의 코스만 추천한다.

넷째, 등산 일정을 관리하고, 다녀온 코스와 장소에 대한 상세 정보를 히스토리 페이지를 구성하여 관리한다.

다섯째, 커뮤니티 기능을 통해 등산객들이 정보를 공유하고 소통할 수 있는 공간을 제공한다.

여섯째, AI를 활용하여 등산 장비 추천 기능을 제공하여 등산 준비를 돕는다.

이를 만족하는 생성형 AI를 이용한 등산 코스 추천 서비스인 "오늘의 등산(Good Morning)"을 개발하였다. 오늘의 등산은 사용자가 원하는 지역과 난이도에 따라 AI가 실시간 날씨 정보를 고려하여 최적의 등산 코스를 생성해 주는 서비스이며 다음과 같은 기능을 요구한다.

**등산 코스 추천 서비스**
   사용자의 지역과 난이도 선호에 맞는 등산 코스를 AI가 추천한다.

**실시간 날씨 정보 서비스**
   각 등산 코스의 현재 날씨 정보를 제공하여 안전한 등산을 돕는다.

**등산 일정 관리 서비스**
   등산 일정을 계획하고 관리할 수 있다.

**커뮤니티 서비스**
   등산객들이 정보를 공유하고 소통할 수 있는 공간을 제공한다.

**스탬프 및 저장 서비스**
   다녀온 산과 코스를 기록하고 관리할 수 있다.

**장비 추천 서비스**
   AI가 등산 목적과 조건에 맞는 장비를 추천한다.

### (3) 기대 효과

공공 데이터를 이용해 전국 등산로 정보를 제공하고, 실시간 날씨 정보를 반영하며, AI가 사용자의 지역과 난이도 선호에 따라 등산 코스를 생성해주는 서비스이다. 이 서비스는 사용자들이 효율적으로 등산 계획을 세우고, 보다 안전하고 즐거운 등산을 할 수 있도록 돕는 것을 목표로 한다.

이 서비스는 등산을 처음 시작하는 초보자들에게 큰 도움이 되며, 자신의 체력 수준에 맞는 코스를 선택할 수 있도록 개인화된 등산 경험을 제공한다. 또한, 사용자는 여러 웹사이트를 검색할 필요 없이 모든 정보를 쉽게 확인할 수 있어 등산 계획에 따른 스트레스를 크게 줄일 수 있다. 실시간 날씨 정보를 통해 위험한 날씨 조건을 사전에 파악하여 안전한 등산을 할 수 있도록 돕는다.

서비스 관리자는 사용자 경향을 분석하고, 인기 있는 등산지를 파악하여 더욱 사용자 친화적인 서비스를 제공할 수 있다. 커뮤니티 기능을 통해 등산객들 간의 정보 공유와 소통이 활성화되어 등산 문화의 확산에도 기여할 수 있다. 이로 인해 등산 산업의 활성화와 지역 관광에도 긍정적인 영향을 미칠 것으로 기대된다.

결론적으로, 본 프로젝트는 사용자에게 효율적이고 안전하며 즐거운 등산 경험을 제공함으로써, 등산 계획의 번거로움을 해소하고 사용자 만족도를 크게 향상시킬 수 있을 것이다. 또한, AI 기술을 활용한 개인화된 추천 시스템은 사용자들이 자신에게 맞는 등산 코스를 쉽게 찾을 수 있도록 하여, 등산의 접근성을 높이고 더 많은 사람들이 등산을 즐길 수 있도록 기여할 것이다.

---

# 2. 프로젝트 설계

## 2.1 기술 설계

### AWS 아키텍처 구성

본 프로젝트는 AWS 클라우드 인프라를 기반으로 구축되었으며, 다음과 같은 AWS 서비스들을 활용한다.

**Security (보안)**
- **AWS IAM**: 사용자 및 서비스의 인증 및 권한 관리
- **AWS Secrets Manager**: 민감한 정보(API 키, 데이터베이스 비밀번호 등)의 안전한 저장 및 관리
- **AWS KMS**: 암호화 키 관리 서비스
- **AWS WAF**: 웹 애플리케이션 방화벽을 통한 보안 강화
- **Amazon Cognito**: 사용자 인증 및 권한 부여 관리
- **AWS ACM**: SSL/TLS 인증서 관리

**AI (인공지능)**
- **Amazon Bedrock**: 생성형 AI를 활용한 등산 코스 추천 및 장비 추천 서비스 구현
- **Amazon Nova**: AWS Bedrock의 최신 생성형 AI 모델로, Knowledge Base를 활용한 지능형 코스 추천 에이전트의 기반 모델로 사용

**Compute (컴퓨팅)**
- **AWS Lambda**: 서버리스 컴퓨팅을 통한 날씨 정보 조회 및 데이터 처리
- **Amazon EKS**: Kubernetes 클러스터를 통한 마이크로서비스 아키텍처 운영
- **Amazon ECR**: Docker 컨테이너 이미지 저장 및 관리
- **Amazon EC2**: GitLab, MongoDB, 모니터링 시스템 등 운영 환경 구축

**Application Integration (애플리케이션 통합)**
- **AWS Systems Manager**: 운영 인사이트 및 자동화
- **Amazon EventBridge**: 이벤트 기반 아키텍처 구축
- **Amazon SQS**: 메시지 큐잉을 통한 비동기 처리
- **AWS CloudTrail**: 거버넌스, 규정 준수 및 감사를 위한 로깅

**Storage (스토리지)**
- **AWS Backup**: 중앙화된 백업 관리
- **Amazon S3**: 객체 스토리지를 통한 정적 파일 및 데이터 저장
- **Amazon EBS**: EC2 인스턴스용 블록 스토리지
- **Amazon S3 Glacier**: 장기 보관을 위한 아카이브 스토리지

**Cost (비용 관리)**
- **AWS Cost Explorer**: AWS 비용 분석 및 관리

**Network (네트워크)**
- **Amazon Route 53**: DNS 웹 서비스
- **Amazon VPC**: 격리된 클라우드 리소스를 위한 가상 네트워크
- **Application Load Balancer (ALB)**: 애플리케이션 로드 밸런싱

**Analysis (분석)**
- **Amazon Athena**: 대화형 쿼리 서비스를 통한 데이터 분석

### 기술 스택 상세

**기본 인프라**
- 기본 시스템은 **Amazon EKS (Elastic Kubernetes Service)**를 기반으로 구축되었다.
- **Istio**를 Service Mesh 도구로 사용하여 마이크로서비스 아키텍처(MSA)를 구현한다.
- **Karpenter**를 활용한 자동 노드 스케일링으로 리소스를 효율적으로 관리한다.

**관찰성 (Observability)**
- **CloudWatch**를 통한 관찰성 구현:
  - 메트릭 수집: **CloudWatch Agent** 및 **Prometheus**를 통한 메트릭 수집
  - 로그 수집: **Fluent Bit**을 통한 로그 수집 및 **Loki**에 저장
  - 트레이싱: **AWS X-Ray**를 통한 분산 추적
- **Grafana**를 통한 메트릭 및 로그 시각화
- **Prometheus**를 통한 메트릭 수집 및 쿼리
- **Loki**를 통한 로그 수집 및 분석

**Infrastructure as Code (IaC)**
- **AWS CloudFormation**을 통한 인프라 코드화 구현
- 인프라 템플릿을 통한 일관된 환경 구축 및 관리

**AI 서비스**
- 등산 코스 추천 서비스는 **Amazon Bedrock**을 활용한 생성형 AI로 구현되었다.
- **Amazon Nova** 모델을 기반으로 한 **AWS Bedrock Agent**를 통해 Knowledge Base에 저장된 등산로 정보를 벡터 검색으로 효율적으로 검색하고, 사용자의 지역과 난이도 선호에 맞는 코스를 추천한다. Amazon Nova는 고성능 생성형 AI 모델로, 정확하고 맥락에 맞는 등산 코스 추천을 제공한다.

**데이터베이스**
- 서비스의 데이터베이스로 **MongoDB**를 선택하여 사용자 정보, 등산 일정, 커뮤니티 게시글, 스탬프 정보 등을 저장한다.
- MongoDB Replica Set을 구성하여 고가용성을 보장한다.

**캐시 및 세션 관리**
- **Redis**를 인메모리 데이터 저장소로 활용하여 빠른 데이터 접근을 제공한다.
- Redis는 이메일 인증번호 저장, 최근 본 상품 기록 등 임시 데이터와 세션 정보를 관리하는 데 사용된다.
- Redis의 Sorted Set 자료구조를 활용하여 최근 본 상품을 시간순으로 정렬하여 저장한다.

**검색 엔진**
- **Elasticsearch**를 검색 엔진으로 사용하여 산 정보, 커뮤니티 게시글, 상품 정보에 대한 고성능 검색 기능을 제공한다.
- Elasticsearch는 오타 허용 검색(Fuzzy Search)을 지원하여 사용자가 입력한 검색어의 오타를 자동으로 보정하여 검색 결과를 제공한다.
- 한국어 불용어(조사, 어미 등)를 자동으로 제거하여 검색 정확도를 향상시킨다.
- 다중 필드 검색을 지원하여 제목, 내용, 위치 등 여러 필드에서 동시에 검색하며, 필드별 가중치를 설정하여 관련도 높은 결과를 우선적으로 제공한다.
- **Kibana**를 Elasticsearch의 관리 및 시각화 도구로 사용하여 인덱스 관리와 검색 쿼리 테스트를 수행한다.

**CI/CD**
- **GitLab**을 별도의 EC2 인스턴스에 구축하여 CI/CD 파이프라인을 운영한다.
- GitLab CI/CD를 통해 자동 빌드, 테스트, 배포를 수행한다.
- **Jenkins**를 Kubernetes 클러스터 내에서 운영하여 추가적인 CI/CD 파이프라인을 제공한다.

**네트워크 구성**
- 운영 환경과 개발 환경을 분리하기 위해 별도의 **VPC**를 사용한다.
- Private Subnet과 Public Subnet을 구분하여 보안을 강화한다.

**협업 도구**
- 일정 관리를 위해 **Jira**를 사용한다.
- 문서 관리를 위해 **Notion**을 사용한다.
- 커뮤니케이션을 위해 **Slack**을 사용하며, 알림 및 보고서 전송에도 활용한다.

**애플리케이션 개발**
- **Backend**는 **Node.js**와 **Express.js** 프레임워크를 사용하여 개발되었다.
- **Frontend**는 **React**를 사용하여 개발되었으며, **Vite**를 빌드 도구로 사용한다.
- **Nginx**를 웹 서버로 사용하여 정적 파일을 서빙한다.

**마이크로서비스 아키텍처**
- 전체 시스템은 마이크로서비스 아키텍처(MSA)로 구성되어 있으며, 다음과 같은 서비스들로 분리되어 있다:
  - **auth-service**: 회원가입 및 인증
  - **community-service**: 커뮤니티 게시글 관리
  - **notice-service**: 공지사항 관리
  - **schedule-service**: 등산 일정 관리
  - **notification-service**: 알림 서비스
  - **store-service**: 스토어 서비스
  - **chatbot-service**: 챗봇 서비스
  - **mountain-service**: 산 정보, 코스, 날씨 정보 제공
  - **ai-service**: AI 등산 코스 추천
  - **ai-infra-service**: 인프라 모니터링 및 AI 분석

**로드 밸런싱 및 API Gateway**
- **HAProxy**를 최상단 로드 밸런서로 사용한다.
- **Traefik**을 API Gateway로 사용하여 서비스 간 라우팅을 관리한다.
- **Istio Gateway**를 통한 외부 트래픽 관리 및 라우팅을 수행한다.

---

## 2.3 서비스 설계

본 서비스는 사용자 중심의 직관적인 인터페이스를 제공하며, 등산 코스 추천부터 일정 관리, 커뮤니티 활동까지 등산 관련 전반적인 기능을 통합적으로 제공한다. 서비스의 주요 사용자 플로우와 기능은 다음과 같이 설계되었다.

### 2.3.1 애플리케이션 초기화 및 인증 플로우

애플리케이션 실행 시, 사용자는 랜딩 페이지를 통해 서비스에 접근한다. 랜딩 페이지에서는 서비스의 주요 기능과 특징을 소개하며, 사용자로 하여금 서비스의 가치를 인지할 수 있도록 구성하였다.

**로그인 플로우**
- 기존 로그인 사용자의 경우, 저장된 인증 토큰을 확인하여 자동으로 메인 화면으로 이동한다. 이를 통해 사용자의 접근성을 향상시키고, 반복적인 로그인 과정을 최소화한다.
- 비로그인 사용자 또는 인증 토큰이 만료된 사용자의 경우, 로그인 화면으로 이동한다. 본 서비스는 소셜 로그인(Kakao, Naver 등)을 지원하여 사용자의 편의성을 높이고, 회원가입 과정을 간소화한다. 소셜 로그인 인증이 성공적으로 완료되면, 사용자는 메인 화면으로 자동 이동한다.
- 회원가입이 필요한 신규 사용자의 경우, 이메일 기반 회원가입을 통해 서비스를 이용할 수 있으며, 이메일 인증을 통해 계정의 유효성을 검증한다.

### 2.3.2 지역 기반 정보 제공

메인 화면에서는 사용자가 원하는 지역을 선택할 수 있는 기능을 제공한다. 사용자는 도, 시, 군, 구, 읍, 면, 동, 리 등 다양한 행정구역 단위로 지역을 선택할 수 있으며, 선택한 지역에 해당하는 등산 정보를 조회할 수 있다. 이는 사용자가 특정 지역을 방문하거나 해당 지역의 등산 정보를 찾고자 할 때 유용한 기능이다.

### 2.3.3 필터링 및 검색 기능

메인 화면에서는 다양한 필터 옵션을 제공하여 사용자가 원하는 정보를 효율적으로 찾을 수 있도록 지원한다.

**필터 옵션**
- **카테고리 선택**: 등산 코스, 산 정보, 커뮤니티 게시글, 스토어 상품 등 다양한 카테고리를 선택하여 검색 범위를 제한할 수 있다.
- **난이도 필터**: 쉬움, 보통, 어려움 등 등산 코스의 난이도를 기준으로 필터링할 수 있다.
- **검색어 입력**: Elasticsearch를 활용한 통합 검색 기능을 통해 산 이름, 게시글 제목, 상품명 등을 검색할 수 있으며, 오타 허용 검색(Fuzzy Search)을 지원하여 사용자 편의성을 향상시킨다.

필터 옵션을 통해 선택된 정보는 메인 화면의 지도에 마커로 표시되며, 사용자는 지도를 통해 해당 지역의 등산 정보를 시각적으로 확인할 수 있다.

### 2.3.4 지도 통합 및 상세 정보 제공

**지도 통합**
- 선택된 필터 옵션에 따라 등산 코스, 산 정보 등이 지도에 마커로 표시된다. 각 마커는 해당 정보의 위치를 정확하게 나타내며, 사용자는 지도를 통해 지역별 등산 정보의 분포를 한눈에 파악할 수 있다.
- 지도 상의 마커를 선택하면, 해당 등산 코스나 산에 대한 상세 정보가 표시된다. 상세 정보에는 코스의 거리, 소요 시간, 난이도, 실시간 날씨 정보 등이 포함되며, 사용자는 이를 바탕으로 등산 계획을 수립할 수 있다.

**실시간 날씨 정보**
- 각 등산 코스의 현재 날씨 정보를 실시간으로 조회하여 제공한다. 날씨 정보에는 온도, 체감 온도, 습도, 풍속, 날씨 상태, 구름량 등이 포함되며, 이를 통해 사용자는 등산에 적합한 날씨인지 판단할 수 있다.
- 위험한 날씨 조건(천둥번개, 강풍 등)이 감지된 코스는 자동으로 필터링되어 추천되지 않으며, 이를 통해 사용자의 안전을 보장한다.

### 2.3.5 AI 기반 등산 코스 추천

**코스 생성 플로우**
- 사용자는 등산 코스를 생성하기 위해 다음 정보를 입력한다:
  - **지역 선택**: 등산하고자 하는 지역을 선택한다.
  - **난이도 선호**: 초보, 보통, 어려움 등 자신의 체력 수준에 맞는 난이도를 선택한다.
  - **테마 선택**: 가벼운 산책, 도전적인 등반, 가족과 함께 등 다양한 테마를 선택할 수 있다.
- 입력된 정보를 바탕으로 **Amazon Bedrock Agent**가 Knowledge Base에 저장된 등산로 정보를 벡터 검색을 통해 검색하고, 사용자의 선호도와 실시간 날씨 정보를 종합적으로 고려하여 최적의 등산 코스를 추천한다.

**AI 추천 결과**
- AI는 사용자에게 최대 3개의 등산 코스를 추천하며, 각 코스에 대해 다음 정보를 제공한다:
  - 산 이름 및 위치 정보
  - 코스 이름, 거리, 소요 시간
  - 난이도 및 난이도 점수
  - 실시간 날씨 정보
  - 코스의 특징 및 AI 추천 설명
- 추천된 코스 중 하나를 선택하면, 해당 코스의 상세 정보가 표시되며, 지도 상에서 코스의 경로를 확인할 수 있다.

**장비 추천**
- 사용자는 등산 목적과 조건을 입력하여 AI로부터 등산 장비 추천을 받을 수 있다. AI는 등산 코스의 난이도, 날씨, 계절 등을 고려하여 적절한 장비를 추천한다.

### 2.3.6 등산 일정 관리

**일정 생성 및 관리**
- 사용자는 추천받은 등산 코스를 기반으로 등산 일정을 생성할 수 있다. 일정에는 등산 날짜, 시간, 참가 인원, 메모 등의 정보가 포함된다.
- 생성된 일정은 마이페이지의 캘린더에 표시되며, 사용자는 일정을 수정하거나 삭제할 수 있다.

**일정 히스토리**
- 사용자가 완료한 등산 일정은 히스토리로 저장되며, 사용자는 히스토리를 통해 과거에 다녀온 등산 코스와 장소에 대한 상세 정보를 확인할 수 있다. 히스토리에는 등산 날짜, 코스 정보, 날씨 정보, 기록한 메모 등이 포함된다.

### 2.3.7 커뮤니티 기능

**게시글 작성 및 조회**
- 사용자는 등산 후기, 정보 공유, 질문 등의 게시글을 작성할 수 있으며, 다른 사용자들의 게시글을 조회하고 댓글을 작성할 수 있다.
- 게시글은 카테고리별로 분류되어 있으며, 검색 기능을 통해 원하는 게시글을 쉽게 찾을 수 있다.

**통합 검색**
- Elasticsearch를 활용한 통합 검색 기능을 통해 산 정보, 게시글, 상품을 동시에 검색할 수 있으며, 검색 결과는 카테고리별로 분류되어 표시된다.

### 2.3.8 스탬프 및 즐겨찾기

**스탬프 기능**
- 사용자가 등산한 산과 코스를 스탬프로 기록할 수 있으며, 스탬프 페이지에서 자신이 다녀온 등산지를 한눈에 확인할 수 있다.
- 스탬프에는 등산 날짜, 코스 정보, 사진 등이 포함되며, 사용자는 이를 통해 자신의 등산 기록을 관리할 수 있다.

**즐겨찾기**
- 사용자는 관심 있는 산이나 코스를 즐겨찾기로 저장할 수 있으며, 마이페이지에서 즐겨찾기 목록을 확인할 수 있다.

### 2.3.9 마이페이지

**기본 기능**
- 마이페이지에서는 사용자의 프로필 정보, 등산 일정, 스탬프, 즐겨찾기, 작성한 게시글 등을 통합적으로 관리할 수 있다.
- 환경 설정을 통해 알림 설정, 계정 정보 수정 등의 기능을 이용할 수 있으며, 로그아웃 기능도 제공한다.

**포인트 및 활동 내역**
- 사용자의 활동 내역(게시글 작성, 댓글 작성 등)을 확인할 수 있으며, 포인트 시스템을 통해 사용자의 활동을 보상한다.

### 2.3.10 서비스 플로우 요약

본 서비스는 다음과 같은 주요 플로우로 구성된다:

1. **초기 접근**: 애플리케이션 실행 → 랜딩 페이지 → 로그인/회원가입 → 메인 화면
2. **정보 탐색**: 지역 선택 → 필터 적용 → 지도에서 정보 확인 → 상세 정보 조회
3. **코스 추천**: 지역 및 난이도 입력 → AI 코스 추천 → 코스 선택 → 상세 정보 및 경로 확인
4. **일정 관리**: 추천 코스 선택 → 일정 생성 → 캘린더에 저장 → 일정 실행 → 히스토리 저장
5. **커뮤니티 활동**: 게시글 작성/조회 → 댓글 작성 → 정보 공유
6. **기록 관리**: 스탬프 기록 → 즐겨찾기 저장 → 마이페이지에서 통합 관리

이러한 플로우를 통해 사용자는 등산 계획 수립부터 실행, 기록 관리까지 전 과정을 하나의 서비스에서 통합적으로 이용할 수 있으며, AI 기반의 개인화된 추천을 통해 자신에게 맞는 등산 경험을 할 수 있다.

---

## 2.4 AWS 클라우드 구성도

본 프로젝트는 AWS 클라우드 인프라를 기반으로 구축되었으며, 고가용성과 보안성을 확보하기 위해 격리된 네트워크 환경과 다중 가용 영역 구성을 채택하였다. AWS 클라우드 구성은 다음과 같이 설계되었다.

### 2.4.1 VPC 네트워크 구성

AWS의 격리된 가상 네트워크 환경인 **Amazon VPC (Virtual Private Cloud)**를 이용하여 서울 리전(ap-northeast-2)에 2개의 VPC를 생성하여 개발 환경과 운영 환경을 물리적으로 분리하여 관리한다. 이를 통해 개발 환경의 변경사항이 운영 환경에 영향을 미치지 않도록 하며, 보안 정책을 환경별로 독립적으로 적용할 수 있다.

**개발 VPC 구성**
- 개발 VPC는 1개의 가용 영역(Availability Zone)을 사용하여 구성한다.
- 개발 환경의 특성상 고가용성보다는 비용 효율성을 우선시하여 단일 가용 영역 구성을 채택하였다.
- 가용 영역 내에 **퍼블릭 서브넷(Public Subnet)**을 생성하여 GitLab CI/CD 서버 등 개발 도구를 배치한다.
- 퍼블릭 서브넷을 통해 외부에서 개발 도구에 직접 접근할 수 있도록 구성하여 개발자의 접근성을 향상시킨다.

**운영 VPC 구성**
- 운영 VPC는 고가용성을 보장하기 위해 2개의 가용 영역을 사용하여 구성한다.
- 각 가용 영역에 **프라이빗 서브넷(Private Subnet)**과 **퍼블릭 서브넷(Public Subnet)**을 생성한다.
- 프라이빗 서브넷에는 애플리케이션 워크로드와 데이터베이스 등 민감한 리소스를 배치하여 외부로부터의 직접적인 접근을 차단한다.
- 퍼블릭 서브넷에는 로드 밸런서, NAT Gateway 등 외부 통신이 필요한 리소스를 배치한다.

### 2.4.2 Kubernetes 클러스터 구성

**EKS 클러스터 배치**
- 운영 VPC의 2개 가용 영역에 위치한 프라이빗 서브넷에 **Amazon EKS (Elastic Kubernetes Service)**가 관리하는 Kubernetes 환경을 구성한다.
- 프라이빗 서브넷에 EKS 클러스터를 배치함으로써 워커 노드와 파드가 외부로부터 직접 접근할 수 없도록 하여 보안을 강화한다.
- 2개의 가용 영역에 분산 배치함으로써 단일 가용 영역 장애 시에도 서비스가 중단되지 않도록 고가용성을 보장한다.

**EKS 클러스터 접근 구성**
- 프라이빗 서브넷에 위치한 EKS 클러스터에 접근하기 위해 **Application Load Balancer (ALB)**를 구성한다.
- ALB는 퍼블릭 서브넷에 배치되어 외부 트래픽을 받아 프라이빗 서브넷의 EKS 클러스터로 라우팅한다.
- EKS 클러스터 내의 파드가 외부 인터넷과 통신하기 위해 각 가용 영역의 퍼블릭 서브넷에 **NAT Gateway**를 구축한다.
- NAT Gateway를 통해 프라이빗 서브넷의 리소스는 외부로 나가는 트래픽만 허용하고, 외부로부터의 직접적인 인바운드 트래픽은 차단하여 보안을 유지한다.

### 2.4.3 모니터링 및 관찰성 구성

**메트릭, 로그, 트레이스 수집**
- 모니터링을 위해 **CloudWatch Agent**, **Fluent Bit**, **AWS X-Ray**를 이용하여 메트릭(Metrics), 로그(Logs), 트레이스(Traces)를 수집한다.
- **CloudWatch Agent**는 EKS 클러스터의 노드 및 파드로부터 CPU, 메모리, 네트워크 등의 메트릭을 수집한다.
- **Fluent Bit**은 컨테이너 로그를 수집하여 중앙화된 로그 관리 시스템으로 전송한다.
- **AWS X-Ray**는 분산 추적을 통해 마이크로서비스 간의 요청 흐름을 추적하고, 성능 병목 지점을 식별한다.

**모니터링 데이터 관리 및 시각화**
- 수집한 메트릭, 로그, 트레이스 데이터는 **Amazon CloudWatch**에서 통합 관리한다.
- CloudWatch를 통해 알람 설정, 대시보드 구성, 로그 분석 등의 기능을 제공한다.
- **Grafana Cloud**를 이용하여 수집한 데이터를 시각화하며, Prometheus와 Loki와 연동하여 실시간 모니터링 대시보드를 구성한다.
- Grafana를 통해 리소스 사용률, 에러율, 응답 시간 등 다양한 지표를 시각적으로 확인할 수 있으며, 임계치 기반 알람을 설정하여 이상 상황을 즉시 감지할 수 있다.

### 2.4.4 CI/CD 파이프라인 구성

**GitLab 구성**
- 개발 VPC의 퍼블릭 서브넷에 EC2 인스턴스를 생성하여 **GitLab**을 구성하여 CI/CD를 관리한다.
- GitLab은 소스 코드 저장소, 이슈 트래킹, 코드 리뷰 등의 기능을 제공하며, GitLab CI/CD를 통해 자동화된 빌드 및 배포 파이프라인을 구축한다.
- GitLab Runner를 통해 코드 변경 시 자동으로 빌드, 테스트, Docker 이미지 생성, ECR 업로드 등의 작업을 수행한다.

**Jenkins 및 ArgoCD**
- Kubernetes 클러스터 내에서 **Jenkins**를 운영하여 추가적인 CI/CD 파이프라인을 제공한다.
- **ArgoCD**를 통해 GitOps 방식의 지속적인 배포를 수행하며, Git 저장소의 변경사항을 자동으로 감지하여 Kubernetes 클러스터에 배포한다.

**컨테이너 이미지 관리**
- 빌드된 Docker 이미지는 **Amazon ECR (Elastic Container Registry)**에 저장되며, EKS 클러스터는 ECR에서 이미지를 Pull하여 파드를 실행한다.

### 2.4.5 인증 및 사용자 관리 구성

**AWS Cognito 구성**
- **AWS Amplify**를 이용하여 **AWS Cognito**를 구성하여 사용자 인증과 회원 관리를 수행한다.
- Cognito User Pool을 통해 사용자 등록, 로그인, 비밀번호 재설정 등의 기능을 제공한다.
- 소셜 로그인(Kakao, Naver 등)을 지원하기 위해 Cognito Identity Provider를 구성한다.

**Lambda 기반 인증 처리**
- **AWS Lambda**와 함께 인증과 회원 관리를 수행하며, Cognito의 인증 이벤트를 Lambda 함수로 처리한다.
- Lambda 함수를 통해 사용자 등록 시 추가 정보 저장, 인증 토큰 커스터마이징 등의 작업을 수행한다.

### 2.4.6 AI 서비스 구성

**Amazon Bedrock 구성**
- **오레곤 리전(us-west-2)**에 있는 **AWS Bedrock**을 이용하여 생성형 AI 작업을 수행한다.
- Bedrock은 Amazon Nova 모델을 기반으로 하며, Knowledge Base를 활용한 지능형 등산 코스 추천 에이전트의 기반 모델로 사용된다.
- 오레곤 리전을 선택한 이유는 Bedrock의 모델 가용성과 비용 효율성을 고려한 것이다.

**API Gateway 및 Lambda를 통한 배포**
- Bedrock에서 생성된 결과 데이터는 **AWS Lambda**와 **API Gateway**를 통해 배포된다.
- Lambda 함수는 Bedrock API를 호출하여 AI 추천 결과를 생성하고, API Gateway를 통해 RESTful API로 제공한다.
- API Gateway는 요청 제한, 캐싱, 인증 등의 기능을 제공하여 안정적인 API 서비스를 보장한다.

### 2.4.7 네트워크 및 보안 구성

**도메인 및 로드 밸런싱**
- **Amazon Route 53**에 사용할 도메인을 등록하고, DNS 레코드를 ALB에 연결한다.
- Route 53을 통해 도메인 이름을 IP 주소로 변환하며, Health Check를 통해 장애 시 자동으로 다른 엔드포인트로 라우팅한다.
- 연결된 ALB는 **AWS ACM (AWS Certificate Manager)**으로 TLS 통신을 하도록 구성하여 HTTPS 프로토콜을 통해 안전한 통신을 보장한다.
- ACM에서 발급한 SSL/TLS 인증서를 ALB에 연결하여 클라이언트와 서버 간의 암호화된 통신을 제공한다.

**웹 애플리케이션 방화벽**
- **AWS WAF (Web Application Firewall)**를 이용하여 웹 기반 공격으로부터 보호한다.
- WAF는 SQL Injection, Cross-Site Scripting (XSS), DDoS 공격 등 다양한 웹 공격을 차단한다.
- WAF 규칙을 통해 허용된 IP 주소, 차단된 IP 주소, 요청 패턴 등을 설정하여 보안 정책을 적용한다.
- WAF는 ALB 앞단에 배치되어 악성 트래픽을 사전에 차단하여 애플리케이션의 보안을 강화한다.

### 2.4.8 백업 전략

**백업 구성**
- 데이터베이스, 로그, 설정 파일 등 중요한 데이터는 정기적으로 백업을 수행한다.
- 백업 데이터는 **Amazon S3**에 저장되며, 장기 보관이 필요한 데이터는 **Amazon S3 Glacier**로 아카이브한다.
- 백업 주기, 보존 기간, 복구 목표 시간(RTO), 복구 목표 지점(RPO)을 정의하여 비즈니스 요구사항에 맞는 백업 전략을 수립한다.

**복구 전략**
- 필요 시 백업된 데이터를 **Amazon EBS**로 복원하여 빠른 복구를 수행한다.
- EBS 스냅샷을 활용하여 특정 시점의 데이터를 복원할 수 있으며, 여러 가용 영역에 스냅샷을 복제하여 재해 복구를 보장한다.

이러한 AWS 클라우드 구성을 통해 본 프로젝트는 고가용성, 보안성, 확장성을 갖춘 안정적인 서비스 인프라를 구축하였으며, 개발 환경과 운영 환경을 분리하여 안전하고 효율적인 개발 및 운영이 가능하도록 설계되었다.

---

## 2.5 AWS EKS 클러스터 구성도

본 프로젝트는 Amazon EKS를 기반으로 Kubernetes 클러스터를 구성하여 마이크로서비스 아키텍처를 구현하였다. EKS 클러스터는 고가용성, 자동 스케일링, 서비스 메시를 통한 트래픽 관리 등의 기능을 제공하여 안정적이고 확장 가능한 애플리케이션 운영을 지원한다.

### 2.5.1 고가용성 및 자동 스케일링 구성

**Horizontal Pod Autoscaler (HPA) 구성**
- 고가용성을 위해 **Horizontal Pod Autoscaler (HPA)**를 구축하여 파드의 자동 스케일링을 구현한다.
- HPA는 CPU 사용률, 메모리 사용률, 커스텀 메트릭 등을 기반으로 파드의 수를 자동으로 조정한다.
- 메트릭 임계치를 초과하면 파드를 증가시키고, 사용률이 낮아지면 파드를 감소시켜 리소스를 효율적으로 관리한다.
- HPA를 통해 트래픽 증가 시 자동으로 파드를 확장하여 서비스의 가용성을 보장하고, 트래픽 감소 시 불필요한 리소스를 해제하여 비용을 최적화한다.

**Karpenter 구성**
- 클러스터 레벨의 자동 스케일링을 위해 **Karpenter**를 구축하여 노드의 자동 스케일링을 구현한다.
- Karpenter는 파드의 리소스 요구사항과 스케줄링 제약 조건을 분석하여 적절한 노드를 자동으로 프로비저닝한다.
- 파드가 스케줄링될 수 없을 때 새로운 노드를 생성하고, 노드의 리소스가 충분히 활용되지 않을 때 노드를 종료하여 클러스터의 리소스 효율성을 향상시킨다.
- Karpenter는 HPA와 함께 작동하여 파드 수가 증가할 때 필요한 노드를 자동으로 추가하고, 파드 수가 감소할 때 불필요한 노드를 제거하여 클러스터의 자동 확장성을 제공한다.

### 2.5.2 서비스 메시 구성

**Istio Service Mesh 구축**
- MSA 환경을 구성하기 위해 AWS EKS에 **Istio Service Mesh**를 구축한다.
- Istio는 마이크로서비스 간의 통신을 관리하고, 트래픽 라우팅, 보안, 관찰성 등의 기능을 제공한다.
- Istio Control Plane은 클러스터의 트래픽 정책과 라우팅 규칙을 관리하며, Istio Data Plane은 각 파드에 사이드카 프록시를 주입하여 실제 트래픽을 처리한다.
- 서비스 메시를 통해 마이크로서비스 간의 통신을 중앙에서 관리할 수 있으며, 서비스 디스커버리, 로드 밸런싱, 장애 복구 등의 기능을 제공한다.

### 2.5.3 인그레스 및 트래픽 라우팅 구성

**Ingress ALB 구성**
- AWS EKS 외부에 있는 ALB의 접근을 위해 AWS EKS 내부에 **Ingress ALB**를 구성한다.
- Ingress ALB는 외부 트래픽을 받아 Kubernetes 클러스터 내부로 라우팅하는 역할을 수행한다.
- ALB Ingress Controller를 통해 Kubernetes Ingress 리소스를 ALB로 변환하여 외부 트래픽을 클러스터 내부로 전달한다.

**Istio Ingress Gateway 및 Virtual Service**
- Ingress ALB에서 Kubernetes 내부에 있는 파드에 접근하기 위해 **Istio Ingress Gateway**로 이동한 후, **Virtual Service**를 통해 목적에 맞는 파드로 이동한다.
- Istio Ingress Gateway는 클러스터의 진입점 역할을 하며, 외부 트래픽을 받아 내부 서비스로 라우팅한다.
- Virtual Service는 트래픽 라우팅 규칙을 정의하며, 요청의 경로, 헤더, 호스트 등을 기반으로 적절한 서비스로 트래픽을 전달한다.
- Virtual Service를 통해 A/B 테스트, 카나리 배포, 트래픽 분산 등의 고급 라우팅 기능을 구현할 수 있다.

### 2.5.4 Istio Proxy Container 구성

**사이드카 프록시 주입**
- 모든 파드들은 **Istio Proxy Container**를 사이드카로 생성하여 네트워크 트래픽을 관리한다.
- Istio Proxy는 Envoy 기반의 사이드카 프록시로, 파드의 모든 인바운드 및 아웃바운드 트래픽을 가로채어 처리한다.
- 사이드카 프록시를 통해 트래픽 암호화, 인증, 라우팅, 로드 밸런싱, 재시도, 서킷 브레이커 등의 기능을 제공한다.
- 네임스페이스 레벨 또는 파드 레벨에서 사이드카 프록시 주입을 제어할 수 있으며, 특정 파드에만 프록시를 주입하거나 제외할 수 있다.

### 2.5.5 네임스페이스 구성 및 역할 구분

**네임스페이스 구조**
- 여러 네임스페이스를 생성하여 역할을 구분하고, 리소스 격리 및 권한 관리를 수행한다.
- 네임스페이스를 통해 논리적으로 리소스를 그룹화하고, 네트워크 정책, 리소스 할당량, RBAC 등을 네임스페이스별로 독립적으로 관리할 수 있다.

**Frontend 네임스페이스 구성**
- Frontend는 **phr-fro-ns**라는 네임스페이스 안에 Flutter 파드를 구성한다.
- Frontend 네임스페이스에는 React 기반의 프론트엔드 애플리케이션 파드가 배포되며, 사용자 인터페이스를 제공한다.
- Frontend 파드는 Istio Ingress Gateway를 통해 외부 트래픽을 받으며, 백엔드 서비스와 통신하여 데이터를 가져온다.

**Backend 네임스페이스 구성**
- Backend는 역할에 따라 여러 네임스페이스로 나누어 구성한다.
- **phr-hrd-ns** 네임스페이스에는 사용자에 대한 정보를 가져오는 SpringBoot 파드를 구성한다. 이 네임스페이스는 사용자 인증, 프로필 관리, 권한 관리 등의 기능을 담당한다.
- **phr-cwr-ns** 네임스페이스에는 경로를 생성하는 SpringBoot 파드를 구성한다. 이 네임스페이스는 등산 코스 생성, 경로 추천, 일정 관리 등의 기능을 담당한다.
- **phr-crd-ns** 네임스페이스에는 장소에 대한 정보를 가져오는 SpringBoot 파드를 구성한다. 이 네임스페이스는 산 정보, 장소 정보, 날씨 정보 등의 기능을 담당한다.
- 각 네임스페이스는 독립적으로 관리되며, 네트워크 정책을 통해 필요한 서비스 간의 통신만 허용하여 보안을 강화한다.

### 2.5.6 모니터링 구성

**DaemonSet을 통한 모니터링 에이전트 배포**
- 모니터링을 위한 **CloudWatch Agent**, **Fluent Bit**, **X-Ray Daemon**을 DaemonSet으로 구성한다.
- DaemonSet은 클러스터의 모든 노드에 하나의 파드를 실행하도록 보장하며, 노드가 추가되면 자동으로 해당 노드에 파드를 배포한다.

**CloudWatch Agent**
- CloudWatch Agent는 각 노드에서 메트릭을 수집하여 Amazon CloudWatch로 전송한다.
- 노드의 CPU, 메모리, 디스크, 네트워크 등의 시스템 메트릭을 수집하며, 커스텀 메트릭도 수집할 수 있다.

**Fluent Bit**
- Fluent Bit은 각 노드에서 로그를 수집하여 중앙화된 로그 관리 시스템으로 전송한다.
- 컨테이너 로그, 애플리케이션 로그, 시스템 로그 등을 수집하며, 로그 파싱, 필터링, 변환 등의 기능을 제공한다.
- 수집된 로그는 Loki나 CloudWatch Logs로 전송되어 저장 및 분석된다.

**X-Ray Daemon**
- X-Ray Daemon은 분산 추적 데이터를 수집하여 AWS X-Ray로 전송한다.
- 마이크로서비스 간의 요청 흐름을 추적하여 성능 병목 지점을 식별하고, 에러 발생 지점을 파악할 수 있다.
- X-Ray를 통해 서비스 맵을 생성하여 마이크로서비스 간의 의존성을 시각화할 수 있다.

### 2.5.7 CI/CD 구성

**GitLab Agent 및 GitLab Runner 구성**
- GitLab의 CI/CD를 위해 **GitLab Agent**와 **GitLab Runner**를 생성한다.
- GitLab Agent는 Kubernetes 클러스터와 GitLab 간의 연결을 제공하며, GitLab에서 클러스터로 직접 배포할 수 있도록 한다.
- GitLab Runner는 CI/CD 파이프라인을 실행하는 에이전트로, Kubernetes Executor를 사용하여 클러스터 내에서 파이프라인 작업을 수행한다.
- GitLab Runner는 코드 변경 시 자동으로 빌드, 테스트, Docker 이미지 생성, ECR 업로드, Kubernetes 배포 등의 작업을 수행한다.

**CI/CD 파이프라인 흐름**
- 개발자가 코드를 GitLab에 푸시하면, GitLab CI/CD 파이프라인이 자동으로 트리거된다.
- 파이프라인은 소스 코드 빌드, 단위 테스트, 통합 테스트, Docker 이미지 빌드, ECR에 이미지 푸시, Kubernetes 매니페스트 업데이트, 클러스터 배포 등의 단계를 수행한다.
- 배포는 ArgoCD를 통해 GitOps 방식으로 수행되며, Git 저장소의 변경사항을 자동으로 감지하여 클러스터에 반영한다.

이러한 EKS 클러스터 구성을 통해 본 프로젝트는 고가용성, 자동 스케일링, 서비스 메시를 통한 트래픽 관리, 모니터링, CI/CD를 통합한 현대적인 클라우드 네이티브 애플리케이션 인프라를 구축하였다.

