# Nova 모델 파싱 에러 우회 방법 (코드 레벨)

## 상황
- Nova 모델만 사용 가능 (Pro/Lite/Micro)
- SDK 최신 버전 사용 중
- Lambda Parser 형식 문제로 실패
- DEFAULT parser가 Nova 응답 파싱 실패

## 해결 방법: 에러 본문에서 응답 데이터 추출

파싱 에러가 발생하더라도, 실제 응답 데이터가 에러 본문에 포함되어 있을 수 있습니다.

### 접근 방법
1. 파싱 에러 발생 시 에러 본문을 자세히 분석
2. 본문에서 실제 응답 텍스트 추출 시도
3. 추출한 텍스트가 있으면 정상 응답으로 처리

### 주의사항
- 에러 본문 형식은 AWS SDK 버전에 따라 다를 수 있음
- 불안정한 해결책일 수 있음
- AWS SDK 업데이트 시 작동하지 않을 수 있음

## 다음 단계
CloudWatch 로그에서 실제 에러 본문 형식을 확인한 후, 코드 수정이 필요합니다.
