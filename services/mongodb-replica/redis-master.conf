bind 0.0.0.0
port 6379
protected-mode no

# 지속성 활성화 (AOF)
appendonly yes
appendfsync everysec

# 필요 시 비밀번호 설정 (환경변수 REDIS_PASSWORD 사용 예시)
# requirepass your_redis_password
# masterauth your_redis_password

